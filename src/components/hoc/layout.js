import React from "react";
import "../../css/base.sass";
import Footer from "../Footer/footer";

const Layout = (props) => {


    return <div>
        {props.children}

    </div>
}

export default Layout;